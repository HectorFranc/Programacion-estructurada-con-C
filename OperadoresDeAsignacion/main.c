#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Declaramos variables
    int x = 10;

    // x = x + 2x
    x += 2 * x;

    // Mostramos el resultado
    printf("X vale: %i", x);

    return 0;
}
